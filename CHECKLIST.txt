╔═══════════════════════════════════════════════════════════╗
║     📋 CHECKLIST CHẠY TEMPMAIL TRÊN LOCAL                 ║
╚═══════════════════════════════════════════════════════════╝

BƯỚC 1: KIỂM TRA PHẦN MỀM
═════════════════════════════════════════════════════════════
[ ] Python 3.9+ đã cài đặt
    → Test: python --version  hoặc  python3 --version
    → Download: https://www.python.org/downloads/

[ ] Node.js 18+ đã cài đặt
    → Test: node --version
    → Download: https://nodejs.org/

[ ] Yarn đã cài đặt
    → Test: yarn --version
    → Cài: npm install -g yarn

[ ] MongoDB đã cài đặt
    → Test: mongosh  hoặc  mongo
    → Download: https://www.mongodb.com/try/download/community


BƯỚC 2: KHỞI ĐỘNG MONGODB
═════════════════════════════════════════════════════════════
[ ] MongoDB đang chạy
    
    macOS:
    → brew services start mongodb-community
    
    Linux:
    → sudo systemctl start mongod
    
    Windows:
    → net start MongoDB
    
    Test kết nối:
    → mongosh  (hoặc mongo)


BƯỚC 3: CHẠY ỨNG DỤNG
═════════════════════════════════════════════════════════════
[ ] Code đã download về máy

[ ] Chọn 1 trong 2 cách:

    ┌─ CÁCH 1: TỰ ĐỘNG (KHUYÊN DÙNG) ────────────────────┐
    │                                                     │
    │  macOS/Linux:                                      │
    │  → bash start_app.sh                               │
    │                                                     │
    │  Windows:                                          │
    │  → Double-click start_app.bat                      │
    │     hoặc chạy: start_app.bat                       │
    │                                                     │
    └─────────────────────────────────────────────────────┘

    ┌─ CÁCH 2: THỦ CÔNG ─────────────────────────────────┐
    │                                                     │
    │  Terminal 1 (Backend):                             │
    │  → cd backend                                       │
    │  → python -m venv venv                              │
    │  → source venv/bin/activate  (Mac/Linux)           │
    │     hoặc venv\Scripts\activate  (Windows)          │
    │  → pip install -r requirements.txt                 │
    │  → python -m uvicorn server:app \                  │
    │      --host 0.0.0.0 --port 8001 --reload           │
    │                                                     │
    │  Terminal 2 (Frontend):                            │
    │  → cd frontend                                      │
    │  → yarn install                                     │
    │  → yarn start                                       │
    │                                                     │
    └─────────────────────────────────────────────────────┘


BƯỚC 4: KIỂM TRA ỨNG DỤNG
═════════════════════════════════════════════════════════════
[ ] Frontend đang chạy
    → Mở: http://localhost:3000
    → Thấy giao diện TempMail

[ ] Backend đang chạy
    → Mở: http://localhost:8001
    → Thấy trang JSON hoặc "404 Not Found" là OK
    → API Docs: http://localhost:8001/docs

[ ] Tạo email thành công
    → App tự động tạo email khi mở
    → Hoặc click nút "Tạo email mới"


TROUBLESHOOTING (NẾU CÓ LỖI)
═════════════════════════════════════════════════════════════
Lỗi: Backend không khởi động
→ Kiểm tra MongoDB đang chạy (mongosh)
→ Kiểm tra venv đã activate (có dấu (venv) ở đầu dòng)
→ Cài lại: pip install -r requirements.txt

Lỗi: Frontend không kết nối Backend
→ Kiểm tra Backend: http://localhost:8001
→ Kiểm tra frontend/.env:
  REACT_APP_BACKEND_URL=http://localhost:8001
→ Restart frontend sau khi sửa .env

Lỗi: Port đã được sử dụng
→ Kill process:
  Mac/Linux: lsof -ti:8001 | xargs kill -9
  Windows: netstat -ano | findstr :8001
           taskkill /PID <PID> /F

Lỗi: Module not found
→ Backend: pip install -r requirements.txt
→ Frontend: yarn install


FILES CẦN KIỂM TRA
═════════════════════════════════════════════════════════════
[ ] backend/.env tồn tại
    Nội dung:
    MONGO_URL=mongodb://localhost:27017
    CORS_ORIGINS=*

[ ] frontend/.env tồn tại
    Nội dung:
    REACT_APP_BACKEND_URL=http://localhost:8001
    PORT=3000


ĐỊA CHỈ QUAN TRỌNG
═════════════════════════════════════════════════════════════
→ Frontend:     http://localhost:3000
→ Backend API:  http://localhost:8001
→ API Docs:     http://localhost:8001/docs


TÍNH NĂNG MỚI (Domain Fix)
═════════════════════════════════════════════════════════════
✅ Email được tạo theo ĐÚNG domain đã chọn
   (Mail.tm và Mail.gw)

⚠️  Guerrilla Mail không hỗ trợ chọn domain
   (Limitation của API)

✅ Response trả về domain đã sử dụng
✅ Logging chi tiết trong backend


HƯỚNG DẪN CHI TIẾT
═════════════════════════════════════════════════════════════
Xem file: HUONG_DAN_CHAY_LOCAL.md


═════════════════════════════════════════════════════════════
✅ Nếu tất cả đều OK, ứng dụng đã sẵn sàng!
   Truy cập: http://localhost:3000
═════════════════════════════════════════════════════════════
