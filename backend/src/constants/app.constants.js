const APP_CONSTANTS = {
  TEMP_EMAIL_TTL_MINUTES: 10,
  HISTORY_RETENTION_HOURS: 24,
  PERSISTENT_MAILBOX_PROVIDERS: ['mailtm', 'mailgw'],
  PROVIDER_COOLDOWN_SECONDS: 60,
  DOMAIN_CACHE_TTL_SECONDS: 300,
  CLEANUP_INTERVAL_MS: 30 * 1000,
};

module.exports = APP_CONSTANTS;
